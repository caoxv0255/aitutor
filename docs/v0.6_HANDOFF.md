# v0.6.0-dev Handoff — 新会话起点

> **当前时间**: 2026-08-04 收尾
> **新会话可直接读这文件 + `git log v0.5.0-dev..v0.6.0-dev`** 了解完整状态

---

## 一、当前状态

**Tag**: `v0.6.0-dev` (推 localtest, uibe + GitHub 待推)

**关键数据**:
- RAG: 28,580 题 / 9 学科 / 2008-2025 / 100% embedded (pgvector 768 dim)
- Frontend: F1 Foundation + F2 Service Layer 完 (39 contract + 2 e2e + lint 0)
- DB: pgvector/pgvector:pg15 (P3, 容器已删, 启动看下方)
- Embed: Ollama nomic-embed-text (Windows 端, 137MB)
- Server: 关 (启动看下方)

**待推 (新会话第一步)**:
```bash
cd /home/cx/aitutor
git push uibe main --tags              # 需 VPN
export GH_TOKEN=ghp_xx...workflow_scope
git push https://x-access-token:${GH_TOKEN}@github.com/caoxv0255/aitutor.git main --tags
```

---

## 二、文件 / 路径 速查

```
[启动]
  /home/cx/aitutor/Makefile                          (16 targets, 优先 make)
  /home/cx/aitutor/scripts/start_pgvector.sh         (pgvector 容器启动)
  /home/cx/aitutor/scripts/ingest_rag_to_pgvector.py (28k 题已 ingest, 跑过)
  /home/cx/aitutor/.env                              (DATABASE_URL + EMBEDDING_PROVIDER=ollama)

[验证]
  /home/cx/aitutor/scripts/test_embedding.mjs        (单 query Ollama 测)
  /home/cx/aitutor/scripts/test_rag_e2e.mjs          (server-side RAG 测)
  /home/cx/aitutor/tests/contract.test.js            (39/39 service 端点)
  /home/cx/aitutor/tests/e2e/demo.spec.js            (2/2 Playwright)

[代码改动 v0.6]
  /home/cx/aitutor/services/embedding.js             (3 provider: local/ollama/remote)
  /home/cx/aitutor/api/core/db.js                    (vector(768), 改 1536→768)
  /home/cx/aitutor/api/core/auth.js                  (dev bypass 加 x-dev-bypass header)
  /home/cx/aitutor/api/routes/rag-search.js          (ingestQuestion 加 content_hash)
  /home/cx/aitutor/database/migrations/005_rag_questions.sql  (vector(768) + 进度表)

[文档 v0.6]
  /home/cx/aitutor/docs/MILESTONES.md                (v0.6.0-dev 基线)
  /home/cx/aitutor/docs/P3_RAG_INGEST.md             (P3 完整流程)
  /home/cx/aitutor/docs/v0.6.0-dev_RELEASE_NOTES.md  (本 release 完整)
  /home/cx/aitutor/docs/frontend-migration/SPEC.md   (F1-F6 6 阶段, v0.2)
  /home/cx/aitutor/docs/frontend-migration/PLAN.md   (16.5 天路线, v0.2)
  /home/cx/aitutor/docs/gaokao_paper_mapping.md      (用卷映射 v0.1, 待 v0.2)

[Frontend]
  /home/cx/aitutor/ai-tutor-frontend/                (主前端, F1+F2 完, F3 未启)
  /home/cx/aitutor/frontend/                         (旧前端, 2-3 周观察后归档)
  /home/cx/aitutor/ai-tutor-frontend/pages/index.html (demo, mock mode)
  /home/cx/aitutor/ai-tutor-frontend/assets/js/api/  (7 services + 37 mock JSON)

[Automation]
  /home/cx/aitutor/Makefile                          (lint / contract / e2e / push / rag / start-pgvector / ingest-pgvector / test-embedding / install-hooks / dev / clean)
  /home/cx/aitutor/scripts/automation/               (push_all + lint + install_hooks)
  /home/cx/aitutor/.git/hooks/                       (pre-commit lint + pre-push ahead/behind)
```

---

## 三、关键命令 (新会话速查)

```bash
# 1. 启服务 (4 步)
make start-pgvector                                    # pgvector 容器
docker cp database/migrations/005_rag_questions.sql pgvector-test:/tmp/005.sql
docker exec pgvector-test psql -U zhiqui -d zhiqui_review -f /tmp/005.sql
NODE_ENV=development EMBEDDING_PROVIDER=ollama node server.js
# → http://localhost:3002 (加 x-dev-bypass: 1 header bypass auth)

# 2. 验证
EMBEDDING_PROVIDER=ollama node scripts/test_embedding.mjs    # Ollama 单 query
node scripts/test_rag_e2e.mjs                                 # server-side RAG 4 query
node tests/contract.test.js                                   # 39/39 contract
node node_modules/@playwright/test/cli.js test tests/e2e/    # 2/2 e2e
make lint                                                    # 0 错

# 3. 测 HTTP 端到端
curl -H "x-dev-bypass: 1" http://localhost:3002/api/rag/search/stats
curl -H "x-dev-bypass: 1" -H "Content-Type: application/json" \
     -X POST http://localhost:3002/api/rag/search/search \
     -d '{"query":"软锰矿制备高锰酸钾","top_k":3,"threshold":0.5}'

# 4. 推 uibe (开校园 VPN)
git push uibe main --tags

# 5. 推 GitHub (user 本机, workflow scope token)
export GH_TOKEN=ghp_xx...workflow_scope
git push https://x-access-token:${GH_TOKEN}@github.com/caoxv0255/aitutor.git main --tags

# 6. 收尾
git tag -d v0.5.0-dev v0.6.0-dev 2>/dev/null  # 可选: 删 local tag
git remote prune origin                       # 清理 stale remote
```

---

## 四、下一步 (新会话拍板)

| 优先级 | 任务 | 工作量 | 价值 |
|---|---|---|---|
| **P1** | 推 uibe + GitHub (上面命令) | 5-10 min | 阶段性完整交付 |
| **P2** | frontend demo 改 ?mock=false 跑真后端 (联调) | 1-2h | 用户能看 RAG 真数据 |
| **P3** | 推 F3 (10 page User Journey 迁移) | 5-7天 | 主前端替换 frontend/ |
| **P4** | 写 v0.2 gaokao_paper_mapping (联网修) | 1-2h | 用卷映射完整版 |
| **P5** | AGE + pgvector 自定义镜像 (A1) | 15-20 min build + 1h ingest | 知识图谱接入 |

**v0.7 路线** (大投入):
- AGE+pgvector 自定义镜像
- RAG↔GraphRAG 双向引用
- ivfflat → HNSW (10K+ 后)
- difficulty / KP 字段反查

---

## 五、Memory 状态 (已存)

新会话 `session_search` 查 "v0.6 RAG pgvector" 或 "aitutor ingest" 即可看到本次细节.

memory 已存:
- v0.6.0-dev 基线 (33 commits, 28k 题)
- P3 决策 (B + Ollama 路线)
- embedding 3 provider
- 已知问题清单

---

**Generated**: 2026-08-04 收尾 | `git log v0.5.0-dev..v0.6.0-dev` 13 commits