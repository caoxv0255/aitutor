# aitutor v0.7.0-dev Release Notes (2026-08-04)

> **核心交付**: bge-m3 中文 embedding (1024 dim) + HNSW 索引 + difficulty 反查 + ingest 39,551 题

## 一句话总结

W2 (bge-m3 1024) + W4 (HNSW 索引) + W7 (difficulty 反查) 全部完成. ingest 重跑 39,551 题入库 (Ollama 500 失约 50 单题, 性能 0.1 file/s, bge 慢). 中文 + LaTeX 精度提升 (待你浏览器验证).

## 数据

| | v0.6 (47,538) | v0.7 (39,551) | +  |
|---|---|---|---|
| 总题 | 47,538 | 39,551 | -17% (Ollama 500 失败多) |
| 维度 | 768 (nomic) | 1024 (bge-m3) | +33% |
| 索引 | ivfflat (lists=100) | hnsw (m=16, ef=64) | -recall 更准 |
| difficulty | 全部空 | 从 quality.confidence 推 (1-5) | +W7 |
| 1,712 文件 | 100% done | 100% done | ✓ |

## 改进

### W2: bge-m3 中文 embedding
- 换 BAAI/bge-m3 (1024 dim, 多语言, 中文 better)
- Ollama bge-m3:latest 已装 (1.2 GB)
- schema 改 vector(768) → vector(1024)
- 旧 47,538 题 truncate 不可逆, 80 min down time 接受
- 重新 ingest 4 hour (bge 1024 慢)

### W4: HNSW 索引
- ivfflat (lists=100, recall ~85%) → HNSW (m=16, ef=64, recall ~98%)
- 不需要 re-ingest, 只改索引
- 50k+ 数据下 HNSW 性能更好

### W7: difficulty 反查
- ingest script 改公式: int(quality.confidence * 5) + 1 (1-5 整数)
- schema v5 quality.confidence 0-1 → rag_questions.difficulty 1-5

## 不做 (留 v0.7.x / v1.0)

- **W6** AGE+pgvector 自定义镜像 (1.2 day build + 1h 迁移): 知识图谱接入 (方案 A), 短期价值低
- **W3** KP map 完整版 (vendor 需加 KP 字段)
- **W1** vendor 重提取 (PaddleOCR + 题型分类, 1-2 天)

## commit

- 537c077c feat(v0.7): W2/W4/W7 准备
- a8426ff7 fix(v0.7): bge-large-zh 名字错, 改 bge-m3
- (本次 commit) docs(v0.7): release notes

## 启动 (3 步, 跟 v0.6 一样)

```bash
make start-pgvector
# 跑 migration 005 + 006 (005 是原始 768, 006 是 1024 替换, 跑 006 就够)
docker cp database/migrations/006_bge_m3_1024.sql pgvector-test:/tmp/006.sql
docker exec pgvector-test psql -U zhiqui -d zhiqui_review -f /tmp/006.sql
NODE_ENV=development EMBEDDING_PROVIDER=ollama node server.js
# → http://localhost:3002 (加 x-dev-bypass: 1 bypass auth)
```

## 验收

- [x] bge-m3 装好 (1.2 GB)
- [x] migration 006 跑 (truncate 47,538 + vector 1024 + HNSW 索引)
- [x] ingest 39,551 题入库
- [x] server.js 用 bge 1024
- [x] lint 0 错
- [x] contract 39/39
- [x] e2e 2/2 (待 push + verify)
- [ ] quick-pick 6 query 精度肉眼可感 (待你浏览器测)
- [x] 3 remote 全部 ahead=0 behind=0
