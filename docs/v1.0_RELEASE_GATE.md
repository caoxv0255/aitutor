# v1.0 发布质量门禁 (Release Gate)

> **日期**: 2026-08-15
> **阶段**: Phase 4 — 工程收敛 (prototype → production)

## 自动门禁 (`npm run gate`)

> 段定义与判据以 `scripts/release-gate.sh` 为准 (本表 2026-09-23 对齐, 共 9 段)。

| # | 段 | 做什么 | 失败含义 | 状态 |
|---|----|--------|----------|------|
| 1 | 单元测试 (vitest) | `npx vitest run --reporter=dot`, 以**退出码**为判据 (grep "passed" 会误判, 失败时也输出该字符串) | 服务层/业务逻辑回归被改坏 | ✅ 641 passed / 4 skipped |
| 2 | 前端 contract (mock) | `node tests/contract.test.js`, mock 后端下校验前后端接口约定 | 前端调用约定与后端不一致, 上真后端取不到数据 | ✅ |
| 3 | Backend Contract Test (真后端) | 自起临时实例 `PORT=3999 SKIP_TASK_WORKER=1 node server.js` (独立进程内存限流桶), 再 `BCT_URL=... node tests/backend-contract.test.js`; 端口被占或未就绪**不回退打主实例** | 真实 HTTP 链路/鉴权/限流回归; 打主实例会污染其限流桶 | ✅ |
| 4 | Docker build | `docker compose -f docker-compose.prod.yml build app` (timeout 600s) + `server.js` 直挂 `/api/` 端点数 ≤ 12 | 生产镜像构建不出来; 或新增端点绕过 `api/modules/*` | ✅ |
| 5 | Health check | `curl $BCT_URL/api/health` 必须含 `"dbReady":true` | 后端没起或数据库连不上, 发布即全站不可用 | ✅ |
| 6 | 仓库一致性 (聚合) | `scripts/check-tracked-refs.mjs` (tracked 引用的本地资源必须已入库) + D079 `.dockerignore` 边界 + `deploy/*.conf` 的 `nginx -t` + 硬编码凭据 + 内部自调用不取自请求头 (SSRF) + mastery 标度 + 仓库 schema 与线上一致 + 新树路由/视觉标准 + AI 输出不进 innerHTML | 干净 clone 跑不起来; 部署模板坏了无人发现; 凭据/SSRF/注入类安全回归 | ✅ |
| 7 | 前端行为测试 (jsdom) | `npm run test:frontend` (`tests/frontend/*`) | 共享层 (`ui.js`/`api.js`/`app.css`) 改动悄悄破坏多页的六态机与错误分类 | ✅ |
| 8 | api.js 契约闸门 | `node tests/frontend/api-contract.test.mjs` (170 项) | 所有页面共用的请求层被改坏, 会批量影响页面 | ✅ |
| 9 | SSE 断连检测 | `scripts/check-no-sse-req-close.mjs`: 流式/SSE handler 内禁止 `req.on('close')`, 例外登记在该脚本的 `ALLOW` | Node 22 下请求体被 `express.json` 读完即触发 `req 'close'` → 整条 SSE 流是空的。写法"看起来很对", review 查不出, 必须机械化拦截 | ✅ |

> 第 3 段与第 5 段依赖后端: 无 `BCT_URL` 时第 5 段直接判红; 调试可用 `SKIP_BCT=1` / `SKIP_DOCKER=1` 跳过第 3/4 段 (pre-commit 门禁即如此), 但**发布前必须跑完整 9 段**。

## 人工门禁 (发布前必做)

| 门禁 | 说明 | 验收标准 |
|------|------|---------|
| Lighthouse | 10 个 F3 页面 Performance / Accessibility / SEO | Performance ≥ 85, a11y ≥ 90 |
| Security scan | 凭证清理 + 依赖漏洞 | `npm audit` 无 high/critical; 无明文密码 in git |
| Cross-browser | Chrome / Edge / Safari / 移动端 | 10 页面渲染无 console error |
| 真数据 smoke | 注册 → 登录 → dashboard → tutor → wrong-book → review 全链路 | 8 步全过 (docs/tutor-real-backend-smoke-test) |

## 已知遗留 (不阻塞发布, 但需排期)

- **lint 基线 2445 项既有债务** (api/ server.js tests/) — 新代码零新增 (已验证), 存量清理另立任务
- **中考知识点 seed 缺失** — 当前仅 gaokao 381 条 (`database/graphify-gaokao-knowledge/textbook_knowledge.json`), 中考数据源待建
- **`hermes-litellm` 容器 unhealthy** (infra 项目, healthcheck 配置问题, 非本仓库)
- **`uibe` remote 含明文密码** — 改用 SSH / credential helper (P1.3)
- **Browser cache 跨 session 黏住** — 已用 no-cache 头缓解, 仍有残余风险 (P0.1)

## 发布流程

```bash
npm run gate                          # 自动门禁全绿
# 人工: lighthouse / security / cross-browser / smoke
git tag v1.0                          # 打 tag
git push origin main --tags           # 推 GitHub mirror
docker compose build && docker compose up -d   # 生产部署
```
