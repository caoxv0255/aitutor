# D087 — P0.3 Dataflow Architecture Decision Gate (D1–D4 FINAL)

**Status:** 🟢 ACCEPTED — human-approved 2026-09-03 (final decision)
**Scope:** architecture freeze only. No code, no schema, no migration, no ingest, no data change.
**Evidence base:** `.ai/audits/p0.3-architecture-decision-gate-2026-09-03.md` (read-only SQL + source trace).

## Decisions (binding)

| ID | Decision | Status |
|---|---|---|
| D1 | Canonical per-user answer fact = **`practice_records`** (Fact Layer) | 🟢 APPROVED |
| D2 | Runtime canonical question identity = **`exam_questions.id`** | 🟢 APPROVED |
| D3 | Vision behavior = **user-confirm**; auto-create allowed only as interim/feature-flag | 🟢 APPROVED |
| D4 | `wrong_questions` stays an **independent curatorial collection / wrong-book**; NOT merged into `practice_records` | 🟢 APPROVED |

## Frozen semantics

```
question_uid          = stable bank identity / dedup key   (exam_questions UNIQUE)
exam_questions.id     = runtime FK identity                 (practice_records.question_id → exam_questions.id)
rag_questions.content_hash = RAG document identity          (shared content index, no user)

practice_records      = FACT LAYER  → weakness aggregation → learning state
wrong_questions       = COLLECTION  → wrong-book / dashboard / suggestions / gamification / legacy consumers
```

### Locked constraint (D4)

`wrong_questions` must **never** become a second fact source for weakness analytics.
Downstream split:

- `practice_records → weak-points / trend / learning state` (single fact path)
- `wrong_questions → wrong-book / UX / counters` (product collection)

## Corrections absorbed from earlier audits

1. `(paper_id, question_number)` is an **ordinary index, NOT UNIQUE** — P0-QB-05 (unique constraint) must be actually created; never assume it exists.
2. `wrong_questions` is **not "never read"** — it has ~7 live product consumers. Only `/api/review/weak-points` ignores it as a weakness fact source.

## Dependency ordering (binding for implementation)

```
QB-P0 Recovery (Gate B)  ← must come FIRST
   ↓ canonical question bank (exam_papers / exam_questions / question_knowledge_points populated)
P0.3 Adapter 1  ← waits for canonical QB (avoid mass question_id = NULL facts)
P0.3 Adapter 2  ← consumer exists; meaningful validation waits for facts
```

**P0.3 Adapter 1 is DESIGN-READY only, NOT to be implemented until the canonical QB gate passes.**

## Current gate status (frozen)

| Gate | Status |
|---|---|
| QB-P0 Atomicity | 🔴 BLOCKED |
| Gate B Recovery | 🟢 READY (step 1 = Recovery Design / Dry-run, no production ingest) |
| P0.3 Step 0 Audit | 🟢 ACCEPTED |
| P0.3 Architecture Decision Gate (D1–D4) | 🟢 ACCEPTED |
| P0.3 Adapter 1 | 🟡 DESIGN READY — implementation blocked on canonical QB |
| P0.3 Adapter 2 | 🟡 consumer exists — validation blocked on facts |
| P0.4 | ⛔ BLOCKED |
