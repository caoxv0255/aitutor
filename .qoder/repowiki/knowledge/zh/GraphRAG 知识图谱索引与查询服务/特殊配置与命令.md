- **启动服务**：`python graphrag_service/main.py`（默认监听 127.0.0.1:8100）。
- **构建索引**：`python graphrag_service/indexer.py index --index <index_name>` 或 `all` 批量构建。
- **环境依赖**：需配置 `DATABASE_URL`、`GRAPHRAG_API_KEY` 等环境变量，并确保系统已安装 `graphrag` CLI。