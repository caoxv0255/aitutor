- 所有数据库操作均通过 `db.get_db()` 上下文管理器获取连接，确保连接在使用后自动关闭。
- 索引与查询功能通过 `subprocess` 封装 GraphRAG CLI 调用，统一在 `run_graphrag_query` 或 `run_graphrag_index` 中处理超时与错误解析。
- 配置项优先从环境变量读取（如 `os.getenv`），并在 `config.py` 中提供默认值，避免硬编码敏感信息。