- 采用 IIFE (立即调用函数表达式) 封装共享工具脚本（如 `components.js`, `theme-utils.js`），避免污染全局命名空间。
- 利用 CSS 自定义属性（CSS Variables）配合 `data-theme` 属性实现全站深色/浅色主题的动态切换。
- 通过检测 `localStorage` 中的 `token` 字段来判断用户登录状态，并据此动态调整导航栏菜单项。
- 在考试模式下，通过监听 `visibilitychange` 事件记录切屏次数，并在达到阈值时触发警告或自动交卷逻辑。