- 所有异步路由处理器均通过 `wrapHandler` 高阶函数包裹，以统一捕获未处理的 Promise 拒绝并返回标准化错误响应。
- API 响应遵循统一格式规范，成功请求使用 `successResponse` 包装数据，失败请求使用 `errorResponse` 包含错误信息。
- 受保护的业务接口（如错题本、学习路径）强制要求通过 `authMiddleware` 进行 JWT 令牌校验，未授权请求直接拦截。
- 后台任务处理采用指数退避重试机制，针对 LLM 调用失败的任务在 `taskWorker` 中按预设延迟（5s, 15s, 45s）重新排队。